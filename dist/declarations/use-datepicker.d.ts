import { UseDatepickerProps, UseDatepicker } from './types';
export declare const useDatepicker: ({ blockedDates, focusOnInit, hasFocusTrap, labels, locale, maxDate, minDate, mode, onChange, onClose, selectDates, weekStart, }: UseDatepickerProps) => UseDatepicker;
